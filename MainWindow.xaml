<Window x:Class="WorkTimeWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        xmlns:lay="clr-namespace:LayUI.Wpf.Controls;assembly=LayUI.Wpf"
        Title="WorkTime - 工作时间记录" Height="829" Width="1390"
        MinHeight="600" MinWidth="1000"
        WindowStartupLocation="CenterScreen"
        Background="#f5f5f5"
        Icon="main.ico"
        Closing="MainWindow_Closing">

    <Window.Resources>
        <!-- 主题配色方案 -->
        <x:Array x:Key="ColorThemes" Type="sys:String" xmlns:sys="clr-namespace:System;assembly=mscorlib">
            <sys:String>Light</sys:String>
            <sys:String>Blue</sys:String>
            <sys:String>Green</sys:String>
            <sys:String>Purple</sys:String>
        </x:Array>

        <!-- 主题颜色定义 -->
        <Style x:Key="LightTheme" TargetType="FrameworkElement">
            <Setter Property="Tag" Value="Light"/>
        </Style>
        <Style x:Key="BlueTheme" TargetType="FrameworkElement">
            <Setter Property="Tag" Value="Blue"/>
        </Style>
        <Style x:Key="GreenTheme" TargetType="FrameworkElement">
            <Setter Property="Tag" Value="Green"/>
        </Style>
        <Style x:Key="PurpleTheme" TargetType="FrameworkElement">
            <Setter Property="Tag" Value="Purple"/>
        </Style>

        <!-- 样式定义 -->
        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2c3e50"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <!-- 简约轻量按钮样式 -->
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#495057"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#f8f9fa"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#e9ecef"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#adb5bd"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CompleteButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Foreground" Value="#28a745"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#d4edda"/>
                    <Setter Property="Foreground" Value="#155724"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#c3e6cb"/>
                    <Setter Property="Foreground" Value="#155724"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DeleteButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Foreground" Value="#dc3545"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#f8d7da"/>
                    <Setter Property="Foreground" Value="#721c24"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#f5c6cb"/>
                    <Setter Property="Foreground" Value="#721c24"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="TimerButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Foreground" Value="#2c3e50"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="16,10"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#e8eaed"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#d1d8e0"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- LayButton样式 - 简约轻量设计 -->
        <Style x:Key="LayButtonStyle" TargetType="lay:LayButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#495057"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="lay:LayButton">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#f8f9fa"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#e9ecef"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#adb5bd"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LayButtonPrimaryStyle" TargetType="lay:LayButton" BasedOn="{StaticResource LayButtonStyle}">
            <Setter Property="Foreground" Value="#2c3e50"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#e8eaed"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#d1d8e0"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="LayButtonDangerStyle" TargetType="lay:LayButton" BasedOn="{StaticResource LayButtonStyle}">
            <Setter Property="Foreground" Value="#dc3545"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#f8d7da"/>
                    <Setter Property="Foreground" Value="#721c24"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#f5c6cb"/>
                    <Setter Property="Foreground" Value="#721c24"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="LayButtonDefaultStyle" TargetType="lay:LayButton" BasedOn="{StaticResource LayButtonStyle}">
            <Setter Property="Foreground" Value="#6c757d"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#e9ecef"/>
                    <Setter Property="Foreground" Value="#495057"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#dee2e6"/>
                    <Setter Property="Foreground" Value="#495057"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 特殊按钮样式 -->
        <Style x:Key="ToggleButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Foreground" Value="#6c757d"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="MinWidth" Value="30"/>
            <Setter Property="MinHeight" Value="30"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#e9ecef"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#dee2e6"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="IconButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#6c757d"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="MinWidth" Value="32"/>
            <Setter Property="MinHeight" Value="32"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#f8f9fa"/>
                    <Setter Property="Foreground" Value="#495057"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#e9ecef"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 主题切换按钮样式 -->
        <Style x:Key="ThemeButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#ffffff"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="3"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <Grid>
                                <ContentPresenter HorizontalAlignment="Center" 
                                                VerticalAlignment="Center"/>
                                <!-- 选中状态的对勾 -->
                                <Path x:Name="CheckMark" 
                                      Data="M6,10 L9,13 L14,6" 
                                      Stroke="{TemplateBinding Foreground}" 
                                      StrokeThickness="2" 
                                      Visibility="Collapsed"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                            <Trigger Property="Tag" Value="Selected">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 各主题按钮的具体颜色 -->
        <Style x:Key="LightThemeButton" TargetType="Button" BasedOn="{StaticResource ThemeButtonStyle}">
            <Setter Property="Background" Value="#f8f9fa"/>
            <Setter Property="BorderBrush" Value="#dee2e6"/>
            <Setter Property="Foreground" Value="#333333"/>
        </Style>

        <Style x:Key="BlueThemeButton" TargetType="Button" BasedOn="{StaticResource ThemeButtonStyle}">
            <Setter Property="Background" Value="#007bff"/>
            <Setter Property="BorderBrush" Value="#0056b3"/>
            <Setter Property="Foreground" Value="#ffffff"/>
        </Style>

        <Style x:Key="GreenThemeButton" TargetType="Button" BasedOn="{StaticResource ThemeButtonStyle}">
            <Setter Property="Background" Value="#28a745"/>
            <Setter Property="BorderBrush" Value="#1e7e34"/>
            <Setter Property="Foreground" Value="#ffffff"/>
        </Style>

        <Style x:Key="PurpleThemeButton" TargetType="Button" BasedOn="{StaticResource ThemeButtonStyle}">
            <Setter Property="Background" Value="#6f42c1"/>
            <Setter Property="BorderBrush" Value="#5a32a3"/>
            <Setter Property="Foreground" Value="#ffffff"/>
        </Style>

        <!-- DataGrid样式 - 文本竖直居中和固定表头 -->
        <Style x:Key="ModernDataGridStyle" TargetType="DataGrid">
            <Setter Property="RowHeight" Value="35"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="AlternatingRowBackground" Value="#f8f9fa"/>
            <Setter Property="CanUserSortColumns" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserResizeColumns" Value="True"/>
            <Setter Property="CanUserReorderColumns" Value="False"/>
            <Setter Property="FrozenColumnCount" Value="0"/>
            <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
            <Setter Property="FontSize" Value="12"/>
            <!-- 更浅的横线颜色 -->
            <Setter Property="HorizontalGridLinesBrush" Value="#f0f0f0"/>
            <Setter Property="VerticalGridLinesBrush" Value="#f0f0f0"/>
            <!-- 边框设置 -->
            <Setter Property="BorderBrush" Value="#dee2e6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <!-- 滚动条设置 -->
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <!-- 鼠标滚轮支持 -->
            <Setter Property="ScrollViewer.PanningMode" Value="Both"/>
            <!-- 冻结标头设置 -->
            <Setter Property="EnableRowVirtualization" Value="True"/>
            <Setter Property="EnableColumnVirtualization" Value="True"/>
            <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True"/>
            <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling"/>
        </Style>

        <!-- DataGridTextColumn样式 - 文本竖直居中，水平左对齐 -->
        <Style x:Key="CenteredTextBlockStyle" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Margin" Value="8,0"/>
            <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 顶部标题栏 -->
        <Border x:Name="HeaderBorder" Grid.Row="0" Background="#2c3e50" Padding="15,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- 时钟图标 -->
                <Canvas Grid.Column="0" Width="24" Height="24" VerticalAlignment="Center" Margin="0,0,10,0">
                    <Path Fill="White" Data="M12,2A10,10 0 0,0 2,12A10,10 0 0,0 12,22A10,10 0 0,0 22,12A10,10 0 0,0 12,2M12,4A8,8 0 0,1 20,12A8,8 0 0,1 12,20A8,8 0 0,1 4,12A8,8 0 0,1 12,4M12.5,7V12.25L17,14.92L16.25,16.15L11,13V7H12.5Z"/>
                </Canvas>
                
                <TextBlock Text="WorkTime" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" Grid.Column="1"/>
                <TextBlock x:Name="CurrentTimeLabel" Text="" FontSize="12" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Right" Grid.Column="2"/>
            </Grid>
        </Border>

        <!-- 主要内容区域 -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" MinWidth="200"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*" MinWidth="400"/>
            </Grid.ColumnDefinitions>

            <!-- 分割器 -->
            <GridSplitter Margin="2,20,2,20" Grid.Column="1" Width="1"  Background="#dddddd" HorizontalAlignment="Center" VerticalAlignment="Stretch" 
                          ResizeBehavior="PreviousAndNext" ShowsPreview="False"/>

            <!-- 左侧任务管理 -->
            <Border Grid.Column="0" Background="White" CornerRadius="5" Padding="10" Margin="0,0,5,0" BorderBrush="#e0e0e0" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- 任务管理Tab -->
                    <lay:LayTabControl x:Name="TaskManagementTabControl">
                        <lay:LayTabControl.Resources>
                            <Style TargetType="lay:LayTabItem">
                                <Setter Property="ContentTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <ContentPresenter Content="{Binding}" />
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </lay:LayTabControl.Resources>
                        <lay:LayTabItem Header="任务列表">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- 任务控制按钮 -->
                                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,5">
                                    <lay:LayButton x:Name="AddTaskButton" Style="{StaticResource LayButtonPrimaryStyle}" Click="AddTaskButton_Click" Margin="0,0,5,0">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="➕" FontSize="14" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="添加任务" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </lay:LayButton>
                                    <lay:LayButton x:Name="DeleteTaskButton" Style="{StaticResource LayButtonDangerStyle}" Click="DeleteTaskButton_Click" IsEnabled="False" Margin="5,0,5,0">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🗑️" FontSize="14" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="删除任务" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </lay:LayButton>
                                </StackPanel>

                                <!-- 任务列表 -->
                                <DataGrid x:Name="TasksDataGrid" Grid.Row="0" 
                                          Style="{StaticResource ModernDataGridStyle}"
                                          SelectionChanged="TasksDataGrid_SelectionChanged"
                                          Sorting="TasksDataGrid_Sorting"
                                          FontFamily="Microsoft YaHei UI"
                                          FontSize="13"
                                          TextOptions.TextFormattingMode="Display"
                                          TextOptions.TextRenderingMode="ClearType"
                                          SnapsToDevicePixels="True"
                                          UseLayoutRounding="True">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="任务名称" Binding="{Binding TaskName}" Width="*" IsReadOnly="True" SortMemberPath="TaskName" ElementStyle="{StaticResource CenteredTextBlockStyle}"/>
                                        <DataGridTextColumn Header="状态" Binding="{Binding StatusDisplayName}" Width="80" IsReadOnly="True" SortMemberPath="TaskStatus" ElementStyle="{StaticResource CenteredTextBlockStyle}"/>
                                        <DataGridTextColumn Header="创建时间" Binding="{Binding CreatedAtDisplay}" Width="100" IsReadOnly="True" SortMemberPath="CreatedAt" ElementStyle="{StaticResource CenteredTextBlockStyle}"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </lay:LayTabItem>
                        
                        <lay:LayTabItem Header="已完成任务">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- 已完成任务列表 -->
                                <DataGrid x:Name="CompletedTasksDataGrid" Grid.Row="0" 
                                          Style="{StaticResource ModernDataGridStyle}"
                                          FontFamily="Microsoft YaHei UI"
                                          FontSize="13"
                                          TextOptions.TextFormattingMode="Display"
                                          TextOptions.TextRenderingMode="ClearType"
                                          SnapsToDevicePixels="True"
                                          UseLayoutRounding="True">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="任务名称" Binding="{Binding TaskName}" Width="*" IsReadOnly="True" ElementStyle="{StaticResource CenteredTextBlockStyle}"/>
                                        <DataGridTextColumn Header="完成时间" Binding="{Binding CompletedAtDisplay}" Width="120" IsReadOnly="True" ElementStyle="{StaticResource CenteredTextBlockStyle}"/>
                                        <DataGridTextColumn Header="总耗时" Binding="{Binding TotalDurationDisplay}" Width="100" IsReadOnly="True" ElementStyle="{StaticResource CenteredTextBlockStyle}"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </lay:LayTabItem>
                    </lay:LayTabControl>
                </Grid>
            </Border>

            <!-- 右侧主要内容 -->
            <Grid Grid.Column="2" Margin="5,0,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- 任务详情区域 -->
                <Border Grid.Row="0" Background="White" CornerRadius="5" Padding="15" Margin="0,0,0,10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- 任务信息 -->
                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock x:Name="TaskNameLabel" Text="未选择任务" FontSize="16" FontWeight="Bold" Grid.Column="0"/>
                            <TextBlock x:Name="TaskStatusLabel" Text="" FontSize="12" Grid.Column="1"/>
                        </Grid>

                        <!-- 计时器控制 -->
                        <Grid Grid.Row="1" Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock x:Name="TimerDisplay" Text="00:00:00" FontSize="24" FontWeight="Bold" 
                                       Foreground="#2c3e50" Grid.Column="0" VerticalAlignment="Center"/>

                            <Button x:Name="TimerButton" Style="{StaticResource TimerButtonStyle}" 
                                    Grid.Column="2" Click="TimerButton_Click" IsEnabled="False" Margin="0,0,10,0">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="⏱️" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="开始计时" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>

                            <Button x:Name="CompleteTaskButton" Style="{StaticResource CompleteButtonStyle}" 
                                    Grid.Column="3" Click="CompleteTaskButton_Click" IsEnabled="False" Visibility="Collapsed">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="✅" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="任务已完成" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </Grid>

                        <!-- 备注输入 -->
                        <Grid Grid.Row="3" Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="备注:" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBox x:Name="NotesTextBox" Grid.Column="1" Margin="0,0,10,0"/>
                            <Button x:Name="SaveNotesButton" Style="{StaticResource ButtonStyle}" 
                                    Grid.Column="2" Click="SaveNotesButton_Click" IsEnabled="False">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="💾" FontSize="14" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="保存备注" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </Grid>
                    </Grid>
                </Border>

                <!-- 时间记录列表 -->
                <Border Grid.Row="1" Background="White" CornerRadius="5" Padding="15" Margin="0,0,0,10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="时间记录" Style="{StaticResource HeaderStyle}" Grid.Column="0"/>
                            <Button x:Name="ToggleTimeRecordsButton" Style="{StaticResource ToggleButtonStyle}" 
                                    Grid.Column="1" Click="ToggleTimeRecordsButton_Click">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="📋" FontSize="12" Margin="0,0,3,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="▼" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </Grid>

                        <Grid Grid.Row="1" x:Name="TimeRecordsHeaderGrid">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock x:Name="TotalDurationLabel" Text="任务总耗时: 00:00:00" 
                                       FontWeight="Bold" Grid.Column="1" VerticalAlignment="Center"/>
                        </Grid>

                        <DataGrid Grid.Row="2" x:Name="TimeRecordsDataGrid" 
                                  Style="{StaticResource ModernDataGridStyle}"
                                  SelectionChanged="TimeRecordsDataGrid_SelectionChanged"
                                  FontFamily="Microsoft YaHei UI"
                                  FontSize="13"
                                  TextOptions.TextFormattingMode="Display"
                                  TextOptions.TextRenderingMode="ClearType"
                                  SnapsToDevicePixels="True"
                                  UseLayoutRounding="True"
                                  MaxHeight="300">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="开始时间" Binding="{Binding StartTimeDisplay}" Width="Auto" IsReadOnly="True" ElementStyle="{StaticResource CenteredTextBlockStyle}"/>
                                <DataGridTextColumn Header="结束时间" Binding="{Binding EndTimeDisplay}" Width="Auto" IsReadOnly="True" ElementStyle="{StaticResource CenteredTextBlockStyle}"/>
                                <DataGridTextColumn Header="持续时间" Binding="{Binding DurationDisplay}" Width="Auto" IsReadOnly="True" ElementStyle="{StaticResource CenteredTextBlockStyle}"/>
                                <DataGridTextColumn Header="备注" Binding="{Binding Notes}" Width="Auto" IsReadOnly="True" ElementStyle="{StaticResource CenteredTextBlockStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>


                <!-- 统计区域 -->
                <Border Grid.Row="2" Background="White" CornerRadius="5" Padding="15">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="统计分析" Style="{StaticResource HeaderStyle}" Grid.Column="0"/>
                            <Button x:Name="ToggleStatisticsButton" Style="{StaticResource ToggleButtonStyle}" 
                                    Grid.Column="1" Click="ToggleStatisticsButton_Click">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="📊" FontSize="12" Margin="0,0,3,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="▼" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </Grid>

                        <!-- 统计信息和控件合并到同一行 -->
                        <Grid Grid.Row="1" x:Name="StatisticsControlsGrid" Margin="0,10,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- 左侧：快速统计信息 -->
                            <Grid Grid.Column="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock x:Name="TotalTimeLabel" Text="总工作时间: 00:00:00" FontWeight="Bold" Margin="0,0,0,5"/>
                                <TextBlock x:Name="ActiveTaskLabel" Text="当前活动任务: 无" FontSize="12"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <TextBlock x:Name="TaskCountLabel" Text="任务数量: 0" FontWeight="Bold" Margin="0,0,0,5"/>
                                <TextBlock x:Name="CompletedTaskCountLabel" Text="已完成: 0" FontSize="12"/>
                                </StackPanel>
                            </Grid>

                            <!-- 右侧：时间段选择和导出按钮 -->
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="时间段:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <ComboBox x:Name="TimePeriodComboBox" Width="120" SelectedIndex="0" SelectionChanged="TimePeriodComboBox_SelectionChanged">
                                    <ComboBoxItem Content="今日"/>
                                    <ComboBoxItem Content="昨日"/>
                                    <ComboBoxItem Content="本周"/>
                                    <ComboBoxItem Content="上周"/>
                                    <ComboBoxItem Content="本月"/>
                                    <ComboBoxItem Content="上月"/>
                                    <ComboBoxItem Content="今年"/>
                                    <ComboBoxItem Content="去年"/>
                                </ComboBox>
                                <Button x:Name="ExportButton" Style="{StaticResource IconButtonStyle}"
                                        Margin="10,0,0,0" Click="ExportButton_Click">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="📤" FontSize="14" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                        <TextBlock Text="导出数据" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </Grid>

                        <!-- 统计Tab -->
                        <lay:LayTabControl Grid.Row="2" x:Name="StatisticsTabControl">
                            <lay:LayTabItem Header="文本统计">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <DataGrid x:Name="TaskStatisticsDataGrid" Grid.Row="0" 
                                              Style="{StaticResource ModernDataGridStyle}"
                                              FontFamily="Microsoft YaHei UI"
                                              FontSize="13"
                                              TextOptions.TextFormattingMode="Display"
                                              TextOptions.TextRenderingMode="ClearType"
                                              SnapsToDevicePixels="True"
                                              UseLayoutRounding="True">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="任务名称" Binding="{Binding TaskName}" Width="*" IsReadOnly="True" ElementStyle="{StaticResource CenteredTextBlockStyle}"/>
                                            <DataGridTextColumn Header="总耗时" Binding="{Binding TotalDurationDisplay}" Width="120" IsReadOnly="True" ElementStyle="{StaticResource CenteredTextBlockStyle}"/>
                                            <DataGridTextColumn Header="会话次数" Binding="{Binding SessionCount}" Width="100" IsReadOnly="True" ElementStyle="{StaticResource CenteredTextBlockStyle}"/>
                                            <DataGridTextColumn Header="状态" Binding="{Binding StatusDisplayName}" Width="100" IsReadOnly="True" ElementStyle="{StaticResource CenteredTextBlockStyle}"/>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </Grid>
                            </lay:LayTabItem>

                            <lay:LayTabItem Header="任务对比图">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <Border Grid.Row="0" Background="#f8f9fa" CornerRadius="3" Padding="10">
                                        <lvc:CartesianChart x:Name="TaskComparisonChart" 
                                                           LegendLocation="Right"
                                                           Hoverable="True"
                                                           DataTooltip="{x:Null}">
                                            <lvc:CartesianChart.AxisX>
                                                <lvc:Axis Title="任务"/>
                                            </lvc:CartesianChart.AxisX>
                                            <lvc:CartesianChart.AxisY>
                                                <lvc:Axis Title="耗时(小时)"/>
                                            </lvc:CartesianChart.AxisY>
                                        </lvc:CartesianChart>
                                    </Border>
                                </Grid>
                            </lay:LayTabItem>

                            <lay:LayTabItem Header="总时长统计">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10" HorizontalAlignment="Right">
                                        <TextBlock Text="图表类型:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                        <ComboBox x:Name="ChartTypeComboBox" Width="120" SelectedIndex="0" SelectionChanged="ChartTypeComboBox_SelectionChanged">
                                            <ComboBoxItem Content="柱状图"/>
                                            <ComboBoxItem Content="折线图"/>
                                        </ComboBox>
                                    </StackPanel>

                                    <Border Grid.Row="1" Background="#f8f9fa" CornerRadius="3" Padding="10">
                                        <lvc:CartesianChart x:Name="TotalDurationChart" 
                                                           LegendLocation="Right"
                                                           Hoverable="True"
                                                           DataTooltip="{x:Null}">
                                            <lvc:CartesianChart.AxisX>
                                                <lvc:Axis Title="时间"/>
                                            </lvc:CartesianChart.AxisX>
                                            <lvc:CartesianChart.AxisY>
                                                <lvc:Axis Title="工作时长(小时)"/>
                                            </lvc:CartesianChart.AxisY>
                                        </lvc:CartesianChart>
                                    </Border>
                                </Grid>
                            </lay:LayTabItem>
                        </lay:LayTabControl>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <!-- 底部状态栏 -->
        <Border x:Name="StatusBarBorder" Grid.Row="2" Background="#34495e" Padding="10,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock x:Name="StatusLabel" Text="就绪" Foreground="White" VerticalAlignment="Center" Grid.Column="0"/>
                
                <!-- 主题切换按钮组 -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Margin="0,0,15,0" FontSize="12" VerticalAlignment="Center">
                        <Hyperlink x:Name="HelpLink" Click="HelpLink_Click" 
                                   Foreground="#ccc" TextDecorations="None">
                            📖 帮助
                        </Hyperlink>
                    </TextBlock>
                    <TextBlock Margin="0,0,15,0" FontSize="12" VerticalAlignment="Center">
                        <Hyperlink x:Name="WuaiPojieLink" Click="WuaiPojieLink_Click" 
                                   Foreground="#ccc" TextDecorations="None">
                            @吾爱破解
                        </Hyperlink>
                    </TextBlock>
                    <Button x:Name="LightThemeButton" Style="{StaticResource LightThemeButton}" Click="ThemeButton_Click" Tag="Light"/>
                    <Button x:Name="BlueThemeButton" Style="{StaticResource BlueThemeButton}" Click="ThemeButton_Click" Tag="Blue"/>
                    <Button x:Name="GreenThemeButton" Style="{StaticResource GreenThemeButton}" Click="ThemeButton_Click" Tag="Green"/>
                    <Button x:Name="PurpleThemeButton" Style="{StaticResource PurpleThemeButton}" Click="ThemeButton_Click" Tag="Purple"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
