<Window x:Class="WorkTimeWPF.TaskInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="添加新任务" Height="200" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#f5f5f5">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="任务名称:" FontSize="14" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,10"/>
        
        <TextBox x:Name="TaskNameTextBox" Grid.Row="1" Height="30" FontSize="14" 
                 Margin="0,0,0,20" KeyDown="TaskNameTextBox_KeyDown"/>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton" Content="取消" Width="80" Height="30" 
                    Margin="0,0,10,0" Click="CancelButton_Click"/>
            <Button x:Name="OkButton" Content="确定" Width="80" Height="30" 
                    Click="OkButton_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
